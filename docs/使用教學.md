# Business Console 使用教學

**平台網址：** https://business-console.zeabur.app  
**版本：** 2026年6月（多帳號版）  
**語言：** 繁體中文

---

## 目錄

1. [登入平台](#1-登入平台)
2. [外撥通話活動](#2-外撥通話活動)
   - 2.1 建立活動範本
   - 2.2 建立新活動
   - 2.3 查看活動狀態
   - 2.4 複製活動
3. [入線熱線設定](#3-入線熱線設定)
   - 3.1 建立入線熱線
   - 3.2 設定知識庫
   - 3.3 查看入線通話
   - 3.4 跟進管理
4. [WhatsApp 訂位確認訊息](#4-whatsapp-訂位確認訊息)
   - 4.1 功能說明
   - 4.2 設定帳號 WhatsApp 確認
   - 4.3 為活動範本啟用確認訊息
   - 4.4 為入線熱線啟用確認訊息
   - 4.5 Twilio 訊息範本設定
5. [WhatsApp 建立外撥活動](#5-whatsapp-建立外撥活動)
   - 5.1 開始使用 WhatsApp 機器人
   - 5.2 透過 WhatsApp 建立活動
   - 5.3 透過 WhatsApp 建立範本
6. [系統管理員（Admin）](#6-系統管理員admin)
   - 6.1 管理員帳號說明
   - 6.2 帳號列表
   - 6.3 新增帳號
   - 6.4 帳號設定（Setup）
   - 6.5 帳號總覽（Overview）
   - 6.6 帳號報告（Report）
   - 6.7 代入帳號（Impersonate）
7. [帳號設定（Settings）](#7-帳號設定settings)
8. [常見問題](#8-常見問題)

---

## 1. 登入平台

**步驟 1.1 — 進入登入頁面**

開啟瀏覽器，前往 https://business-console.zeabur.app/login

```
[截圖 1.1: 登入頁面，顯示「business-console」標題、
用戶名及密碼輸入欄位及「登入」按鈕]
```

**步驟 1.2 — 輸入用戶名及密碼**

在「**用戶名**」欄位輸入您的帳號名稱，在「**密碼**」欄位輸入密碼，然後點擊「**登入**」按鈕。

> **提示：** 如忘記密碼，請聯絡系統管理員（admin 帳號）重設。

**帳號類型說明：**

| 帳號類型 | 說明 |
|----------|------|
| **一般帳號** | 登入後進入主畫面（外撥活動列表），只能查看自己的資料 |
| **管理員帳號** | 登入後進入管理員頁面（/admin），可管理所有帳號 |

**步驟 1.3 — 主畫面**

一般帳號登入成功後，將進入主畫面（外撥活動列表）。

```
[截圖 1.3: 主畫面，左側顯示「外撥」和「入線」導航選單，
右側顯示活動列表及頂部的行業範本選擇區]
```

**介面說明：**

| 區域 | 說明 |
|------|------|
| 左側導航 | 外撥活動、入線熱線、設定、登出 |
| 頂部範本列 | 快速選擇行業（餐廳、美容院、保險等）|
| 活動列表 | 顯示進行中、已完成、草稿的活動 |
| 右上角「新增」| 建立新活動或新熱線 |

> **注意：** 若左側導航顯示「**Admin**」連結，表示您使用管理員帳號登入。

---

## 2. 外撥通話活動

外撥通話讓 AI 自動致電您的客戶，進行訂座確認、預約提醒等工作。

### 2.1 建立活動範本

範本儲存常用的 AI 語音及腳本設定，下次建立活動時直接選用，無需重新設定。

**步驟 2.1.1 — 進入範本管理**

點擊頂部導航的「**管理範本**」連結（在新增活動頁面的第一步），或透過主畫面進入。

```
[截圖 2.1.1: 管理範本頁面，顯示系統內建範本列表
（餐廳、美容院、保險、旅行社、醫療診所、地產）
以及「+ 新增範本」按鈕]
```

**步驟 2.1.2 — 新增自訂範本**

點擊「**+ 新增範本**」按鈕，填寫以下資料：

```
[截圖 2.1.2: 新增範本表單，顯示各輸入欄位]
```

| 欄位 | 說明 | 範例 |
|------|------|------|
| **Emoji** | 範本圖示 | 🍽️ |
| **範本名稱** * | 識別用名稱 | 餐廳週末版 |
| **行業** | 選擇行業分類 | 餐廳 |
| **語音** | AI 語音選擇 | Jamie（女聲粵語）|
| **問候語** | AI 接通後第一句話 | 您好，我係XXX嘅Jamie... |
| **AI 腳本** | AI 對話指示 | 詳見下方說明 |
| **啟用範本 WhatsApp 確認** | 此範本的活動完成確認後，自動發送 WhatsApp 訊息給客人 | 開啟/關閉 |

**問候語及腳本的變數說明：**

您可以在問候語和腳本中使用以下變數，系統會在致電時自動替換為實際資料：

| 變數 | 說明 |
|------|------|
| `{{name}}` | 客人姓名 |
| `{{date}}` | 預訂日期 |
| `{{time}}` | 預訂時間 |
| `{{party_size}}` | 人數/備註 |
| `{{business}}` | 您的商戶名稱 |

**範例問候語（餐廳訂座確認）：**
```
您好，我係{{business}}嘅Jamie。我哋打嚟確認您
{{date}}{{time}}，{{party_size}}位嘅訂座。
請問您係咪如期到來？
```

**步驟 2.1.3 — 儲存範本**

填寫完畢後，點擊「**儲存範本**」按鈕。範本將出現在列表中，標示為「自訂」。

```
[截圖 2.1.3: 範本已儲存，列表顯示新建立的範本
帶有紫色「自訂」標籤]
```

---

### 2.2 建立新活動

**步驟 2.2.1 — 開始建立活動**

在主畫面點擊右上角「**+ 新增**」按鈕，或點擊頂部行業範本區的「**新增活動**」。

```
[截圖 2.2.1: 新增活動頁面，顯示四個步驟區域：
1. 範本、2. 活動、3. 預訂、4. 何時致電？]
```

---

**第一步：選擇範本**

```
[截圖 2.2.2: 第一步範本選擇，顯示下拉選單和範本腳本預覽]
```

1. 點擊下拉選單，選擇您要使用的範本（例如「🍽️ 餐廳」）
2. 選擇後，下方會顯示該範本的腳本預覽
3. 若需要新增或修改範本，點擊右側「**管理範本**」連結

> **提示：** 系統已預設 6 個行業範本（餐廳、美容院、保險、旅行社、醫療診所、地產），開箱即用。

---

**第二步：活動設定**

```
[截圖 2.2.3: 第二步活動設定，顯示活動名稱、
預訂日期和預訂時間欄位]
```

| 欄位 | 說明 |
|------|------|
| **活動名稱** | 系統自動填入（格式：DDMMYYYYHHMMSS），可自行修改 |
| **預訂日期** | 所有聯絡人的預設預訂日期（例如：2026-05-31）|
| **預訂時間** | 所有聯絡人的預設預訂時間（例如：07:00 PM）|

> **注意：** 預訂日期和時間會對應腳本中的 `{{date}}` 和 `{{time}}`。若個別聯絡人有不同時間，可在第三步的「預訂時間」欄位單獨設定。

---

**第三步：新增預訂記錄**

```
[截圖 2.2.4: 第三步預訂記錄，顯示「Booking 1」輸入表單，
包含客人姓名、電話（附區碼選擇器）、
預訂時間、預訂日期和備註欄位]
```

每筆預訂記錄包含以下欄位：

| 欄位 | 說明 | 範例 |
|------|------|------|
| **客人姓名** | 致電時使用 `{{name}}` 替換 | 陳先生 |
| **電話** | 附區碼選擇器，預設 🇭🇰 +852 | 51873117 |
| **預訂時間** (選填) | 覆蓋活動預設時間 | 7pm |
| **預訂日期** (選填) | 覆蓋活動預設日期 | 30-May |
| **備註** (選填) | 對應 `{{party_size}}` | 4位，靠窗 |

**新增多筆預訂：**

點擊「**+ 新增預訂**」按鈕，可新增更多聯絡人。

```
[截圖 2.2.5: 顯示多筆預訂記錄，以及「+ 新增預訂」按鈕]
```

**批量匯入聯絡人（CSV）：**

若聯絡人較多，可使用 CSV 檔案批量匯入：

1. 點擊「**下載範本**」→「完整 (5 欄) ↓」下載範本
2. 用 Excel 或 Numbers 打開，填寫資料後儲存
3. 點擊「**匯入 CSV**」上傳檔案

```
[截圖 2.2.6: CSV 範本格式，顯示五欄標題：
Name, Telephone, Schedule, Date, Remark]
```

CSV 格式範例：
```
Name,Telephone,Schedule,Date,Remark
陳大文,51873117,7pm,30-May,4位
李小明,90218835,6pm,30-May,2位
王美華,98765432,,,靠窗
```

**批量匯入聯絡人（圖片）：**

您也可以直接上傳手寫或印刷的聯絡人名單圖片：

1. 點擊「**匯入圖片**」
2. 選擇聯絡人名單的相片（支援手寫名單）
3. AI 自動識別並提取姓名、電話及備註

```
[截圖 2.2.7: 圖片匯入功能，顯示上傳按鈕及
AI 正在提取聯絡人的載入狀態]
```

---

**第四步：設定致電時間**

```
[截圖 2.2.8: 第四步致電時間選擇，顯示兩個選項卡：
「立即開始（確認後立即開始致電）」
和「排程致電（選擇日期及時間）」]
```

**選項一 — 立即開始：**
點擊「**立即開始**」卡片（綠色邊框），確認後 AI 即時開始致電所有聯絡人。

**選項二 — 排程致電：**
點擊「**排程致電**」卡片，在下方出現的日期時間選擇器中設定致電時間。

---

**確認並啟動活動：**

```
[截圖 2.2.9: 頁面底部的活動摘要欄，顯示：
✅ 準備啟動 / ⚠️ 新增預訂以啟動
聯絡人數量・日期時間・範本名稱
「確認並啟動活動」按鈕]
```

頁面底部會顯示活動摘要：
- ✅ 綠色代表已填寫必要資料，可以啟動
- ⚠️ 黃色代表尚需新增預訂記錄

確認無誤後，點擊「**確認並啟動活動**」按鈕。

---

### 2.3 查看活動狀態

**步驟 2.3.1 — 活動列表**

回到主畫面，活動列表顯示所有活動的狀態：

```
[截圖 2.3.1: 主畫面活動列表，顯示「進行中」標籤頁，
各活動卡片顯示名稱、狀態標籤、聯絡人數及進度條]
```

**標籤頁說明：**

| 標籤 | 顯示的活動 |
|------|----------|
| **進行中** | 正在致電（Running）、已暫停（Paused）、已排程（Scheduled）|
| **已完成** | 所有致電已完成（Done）|
| **草稿** | 尚未啟動（Draft）|
| **全部** | 所有活動 |

**活動狀態標籤說明：**

| 標籤 | 顏色 | 說明 |
|------|------|------|
| 進行中 | 綠色 | AI 正在致電 |
| 已暫停 | 黃色 | 已手動暫停 |
| 已排程 | 藍色 | 等待排程時間 |
| 草稿 | 灰色 | 尚未啟動 |
| 已完成 | 灰色 | 全部完成 |

**步驟 2.3.2 — 查看活動詳情**

點擊任一活動卡片進入詳情頁：

```
[截圖 2.3.2: 活動詳情頁，顯示進度條、
四個標籤頁（概覽/聯絡人/報告/設定）
及右上角的操作按鈕（新增聯絡人、暫停、···）]
```

**概覽 (Overview) 標籤：**
```
[截圖 2.3.3: 概覽標籤，顯示六個統計卡片：
已致電、已接聽（含百分比）、未接聽、語音信箱、忙線、待致電]
```

**聯絡人 (Contacts) 標籤：**
```
[截圖 2.3.4: 聯絡人標籤，顯示聯絡人表格：
姓名、電話、狀態（Pending/Done）、結果（Answered/No Answer）、時長]
```

**報告 (Reports) 標籤：**
點擊「報告」標籤或「查看報告」連結進入通話報告列表：

```
[截圖 2.3.5: 通話報告列表，顯示每通通話的
聯絡人、時間、時長、結果（Answered/Voicemail）、情緒（Positive/Neutral/Negative）]
```

點擊「**查看**」可查看個別通話的詳細記錄：

```
[截圖 2.3.6: 單筆通話報告頁，顯示：
客人姓名及電話、通話結果標籤、摘要、
關鍵點列表、完整對話文字記錄]
```

**步驟 2.3.3 — 活動操作**

在活動詳情頁右上角點擊「**···**」可進行以下操作：

```
[截圖 2.3.7: 操作選單展開，顯示所有操作選項]
```

| 操作 | 說明 |
|------|------|
| **新增聯絡人** | 為現有活動添加新的致電聯絡人 |
| **暫停** | 暫停正在進行的活動 |
| **繼續** | 繼續已暫停的活動 |
| **匯入聯絡人** | 透過 CSV 批量匯入聯絡人 |
| **查看報告** | 進入通話報告頁面 |
| **複製活動** | 複製活動配置（語音、腳本）為新草稿 |
| **儲存為範本** | 將當前配置保存為範本 |
| **重設及重試** | 將所有聯絡人重設為待致電狀態 |
| **匯出 CSV** | 匯出聯絡人及通話結果 |
| **刪除活動** | 永久刪除活動（不可復原）|

---

### 2.4 複製活動（重複使用）

若您需要對相同聯絡人群體定期致電（例如每週確認訂座），可使用「複製活動」功能：

**步驟 2.4.1 — 複製**

在活動詳情頁，點擊「**···**」→「**複製活動**」

系統會建立一個包含相同腳本和語音設定的新草稿，並自動跳轉至聯絡人匯入頁面。

```
[截圖 2.4.1: 複製後的新活動頁面，顯示
活動名稱帶有「(copy)」後綴，處於草稿狀態]
```

**步驟 2.4.2 — 新增新一批聯絡人**

匯入新的聯絡人 CSV 或逐一輸入，然後啟動活動。

---

## 3. 入線熱線設定

入線熱線讓 AI 自動接聽客戶來電，回答問題、接受訂位，並在非辦公時間留言。

### 3.1 建立入線熱線

**步驟 3.1.1 — 進入入線管理**

在左側導航點擊「**入線**」。

```
[截圖 3.1.1: 入線頁面，顯示現有熱線列表
及右上角「+ 新增熱線」按鈕]
```

**步驟 3.1.2 — 新增熱線**

點擊「**+ 新增熱線**」按鈕。

```
[截圖 3.1.2: 建立入線熱線表單，顯示各設定欄位]
```

填寫以下資料：

**基本設定：**

| 欄位 | 說明 | 範例 |
|------|------|------|
| **行業範本** | 選擇預設行業（自動填入系統提示）| 🍽️ 餐廳 |
| **熱線名稱** * | 識別用名稱 | 主要訂座熱線 |
| **Twilio 號碼** * | 附區碼選擇器 + 電話號碼 | 🇭🇰 +852 / 26715377 |
| **語音** | AI 語音選擇 | Jamie（女聲粵語）|

**AI 設定：**

| 欄位 | 說明 |
|------|------|
| **系統提示** | AI 的行為指示，說明如何回應客人 |
| **營業時間** | 設定辦公時間（預設週一至週六）|
| **非營業時間 AI 指示** | 辦公時間外的 AI 回應方式 |

**系統提示範例（餐廳）：**
```
你係{{business}}嘅友善主持人。協助來電者處理訂座、
查詢營業時間、菜單問題及特別要求。保持簡潔、
熱情及專業。如有需要請轉交員工跟進。
```

**步驟 3.1.3 — 儲存熱線**

點擊「**建立熱線**」按鈕。

```
[截圖 3.1.3: 建立成功後跳轉至熱線詳情頁，
顯示「啟用中」狀態標籤及四個標籤頁]
```

**步驟 3.1.4 — 設定 Twilio 號碼 Webhook**

在 Twilio 後台，將您電話號碼的語音 Webhook 設定為：
```
https://business-console.zeabur.app/api/twiml/inbound
```

> **重要：** 完成此步驟後，AI 才能接聽來電。

---

### 3.2 設定知識庫

知識庫讓 AI 能正確回答關於您業務的問題（營業時間、服務項目等）。

**步驟 3.2.1 — 進入知識庫標籤**

在熱線詳情頁點擊「**知識庫**」標籤。

```
[截圖 3.2.1: 知識庫標籤頁，顯示系統自動建立的預設文章
（餐廳介紹、營業時間、訂座政策、常見問題）]
```

> **提示：** 系統會根據您選擇的行業範本，自動建立 4 篇預設知識庫文章。

**步驟 3.2.2 — 編輯文章內容**

預設文章包含通用範本，您需要將 `{{business}}` 等佔位符替換為您的實際資料。

點擊任一文章卡片可進行編輯（目前版本需透過刪除後重新新增）。

**步驟 3.2.3 — 新增文章**

點擊「**+ 新增文章**」按鈕，可手動輸入文章；或使用「**匯入 PDF / 文字**」功能批量匯入：

**方法 A — 手動輸入：**

```
[截圖 3.2.3: 新增文章表單，顯示標題和內容欄位]
```

| 欄位 | 說明 | 範例 |
|------|------|------|
| **文章標題** | 主題標題 | 我們的菜單 |
| **文章內容** | 詳細資訊 | 我們提供廣東料理... |

**方法 B — 匯入 PDF 或文字（推薦用於大量內容）：**

```
[截圖 3.2.4: 知識庫匯入區，顯示標題輸入欄、
文字貼上區（方法A）及 PDF 上傳按鈕（方法B）]
```

1. 在「**標題**」欄位輸入文章名稱
2. **文字方式**：將文件文字貼入文字框，點擊「**匯入**」
3. **PDF 方式**：點擊「**Choose File**」選擇 PDF 檔案，點擊「**匯入**」

> **注意：** PDF 匯入僅支援文字型 PDF（非掃描圖片）。系統會自動分段儲存並建立向量索引供 AI 搜尋使用。

> **最佳實踐：** 建議建立以下文章：
> - 商戶介紹（地址、特色）
> - 實際營業時間
> - 服務項目及收費
> - 常見問題解答

---

### 3.3 查看入線通話

**步驟 3.3.1 — 熱線列表**

在左側導航點擊「**熱線**」，查看所有熱線的狀態：

```
[截圖 3.3.1: 熱線列表，顯示各熱線卡片，
包含熱線名稱、電話號碼、狀態（啟用中/已暫停）]
```

**步驟 3.3.2 — 查看即時通話**

點擊熱線，進入「**即時**」標籤：

```
[截圖 3.3.2: 即時標籤，無來電時顯示空白狀態；
有來電時顯示來電號碼及通話時長]
```

**步驟 3.3.3 — 查看報告**

點擊「**報告**」標籤：

```
[截圖 3.3.3: 報告標籤，顯示四個統計卡片
（總通話數、平均時長、已解決百分比、已轉介數）
及「需要跟進」區域和「最近通話」列表]
```

**統計說明：**

| 統計項目 | 說明 |
|----------|------|
| **總通話數** | 該熱線接聽的總通話次數 |
| **平均時長** | 每通通話的平均秒數 |
| **已解決 %** | AI 成功解決客人問題的比例 |
| **已轉介** | 客人要求轉接真人的次數 |

**需要跟進區域：**

非辦公時間來電會顯示在「**需要跟進**」區域（橙色邊框）：

```
[截圖 3.3.4: 需要跟進區域，顯示非辦公時間來電的
客人電話、通話時間及通話摘要]
```

點擊「**查看對話記錄**」可查看完整通話記錄（按鈕僅在有記錄時顯示）。

---

### 3.4 跟進管理

**步驟 3.4.1 — 標記跟進狀態**

每筆需要跟進的通話都有三個快速操作按鈕：

```
[截圖 3.4.1: 跟進記錄卡片，顯示通話摘要、
三個狀態按鈕（已確認預訂/已回電/毋需跟進）
及備註輸入欄位和儲存按鈕]
```

| 按鈕 | 說明 |
|------|------|
| **✅ 已確認預訂** | 已聯絡並確認預訂 |
| **📞 已回電** | 已回電客人 |
| **❌ 毋需跟進** | 無需進一步跟進 |

**步驟 3.4.2 — 新增備註**

在備註欄位輸入說明（例如：「已確認星期五晚7時2位」），然後點擊「**儲存**」。

標記完成後，記錄會變淡並顯示狀態標籤，方便識別已處理的跟進。

```
[截圖 3.4.2: 已標記跟進狀態的記錄，顯示淡化效果
及對應狀態標籤（例如「已確認預訂」）]
```

---

## 4. WhatsApp 訂位確認訊息

當客人在電話中確認訂位後，系統可自動發送 WhatsApp 訊息給客人，確認預訂詳情。

### 4.1 功能說明

**觸發條件：**

| 場景 | 觸發條件 |
|------|----------|
| **外撥通話** | AI 判斷客人已確認訂座（outcome = booking_confirmed）|
| **入線通話** | AI 判斷客人已確認訂座，且入線 WhatsApp 確認已啟用 |

**訊息內容（根據 Twilio 範本）：**

```
📋 {{restaurant}}訂位通知

親愛的 {{customer}}，

您的訂位已{{status}}：

📅 日期：{{date}}
🕡 時間：{{time}}
👥 人數：{{people}} 位

期待您的光臨，如有任何查詢，歡迎隨時聯絡我們。謝謝！
```

> **重要：** WhatsApp 確認訊息使用 Twilio Content Template 發送，需先在 Twilio 後台建立訊息範本（詳見 4.5 節）。

---

### 4.2 設定帳號 WhatsApp 確認

每個帳號獨立控制 WhatsApp 確認功能，設定儲存於該帳號資料中。

**步驟 4.2.1 — 進入設定頁面**

在左側導航點擊「**設定**」。

```
[截圖 4.2.1: 設定頁面，顯示「WhatsApp 訂位確認」區塊]
```

**步驟 4.2.2 — 填寫設定**

| 欄位 | 說明 | 範例 |
|------|------|------|
| **商戶名稱** | 顯示於 WhatsApp 訊息的餐廳名稱（對應 `{{restaurant}}`）| 海景餐廳 |
| **外撥通話確認** | 外撥通話確認訂位後自動發送 WhatsApp | 開啟 ✅ |
| **入線通話確認** | 入線通話確認訂位後自動發送 WhatsApp | 開啟/關閉 |

**步驟 4.2.3 — 儲存設定**

點擊「**儲存設定**」按鈕。

> **注意：** 帳號設定必須開啟，各範本/熱線的個別設定才會生效。

---

### 4.3 為活動範本啟用確認訊息

除全域設定外，每個活動範本也有獨立的開關。只有當「全域外撥確認」**及**「範本 WhatsApp 確認」**均開啟**時，才會發送訊息。

**步驟 4.3.1 — 進入範本編輯**

在主畫面點擊「**管理範本**」→ 選擇或建立範本。

```
[截圖 4.3.1: 範本編輯頁面底部，顯示
「啟用範本 WhatsApp 確認」開關及說明文字]
```

**步驟 4.3.2 — 啟用開關**

將「**啟用範本 WhatsApp 確認**」開關設為開啟（紫色）。

**步驟 4.3.3 — 儲存範本**

點擊「**儲存範本**」。

> **提示：** 系統內建的 6 個行業範本（餐廳、美容院等）預設**未啟用**此功能，請按需手動開啟。

---

### 4.4 為入線熱線啟用確認訊息

**步驟 4.4.1 — 進入熱線設定**

在熱線詳情頁點擊「**設定**」標籤。

**步驟 4.4.2 — 啟用 WhatsApp 確認**

找到「**WhatsApp 訂位確認**」開關，設為開啟。

> **前置條件：** 全域設定中的「**入線通話確認**」也必須開啟。

---

### 4.5 Twilio 訊息範本設定

WhatsApp 確認訊息透過 Twilio Content Template 發送，需在 Twilio 後台建立範本。

**步驟 4.5.1 — 在 Twilio 建立 Content Template**

1. 登入 [Twilio Console](https://console.twilio.com)
2. 前往 **Messaging → Content Template Builder**
3. 點擊「**Create new template**」
4. 選擇「**Custom**」類型
5. 填寫範本內容，使用以下**具名變數**（必須與以下名稱完全一致）：

```
📋 {{restaurant}}訂位通知

親愛的 {{customer}}，

您的訂位已{{status}}：

📅 日期：{{date}}
🕡 時間：{{time}}
👥 人數：{{people}} 位

期待您的光臨，如有任何查詢，歡迎隨時聯絡我們。謝謝！
```

> **重要：** 變數名稱必須使用 `{{restaurant}}`、`{{customer}}`、`{{status}}`、`{{date}}`、`{{time}}`、`{{people}}`，系統才能正確填入資料。

6. 提交範本待 WhatsApp 審核（通常數小時內完成）
7. 審核通過後，複製範本的 **Content SID**（格式：`HXxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`）

**步驟 4.5.2 — 設定範本 SID（進階）**

若您有多個商戶使用不同的 Twilio 範本，可在管理員後台的帳號設定中，為每個帳號設定專屬的 Twilio WhatsApp 號碼及帳號憑證。

**步驟 4.5.3 — 確認訊息變數對應**

系統發送 WhatsApp 時的變數對應如下：

| 範本變數 | 資料來源 |
|----------|----------|
| `{{restaurant}}` | 設定 → 商戶名稱 |
| `{{customer}}` | 聯絡人姓名 |
| `{{status}}` | 固定為「已確認」 |
| `{{date}}` | 聯絡人的預訂日期（`custom_data.date`）或 AI 從通話中提取 |
| `{{time}}` | 聯絡人的預訂時間（`custom_data.time`）或 AI 從通話中提取 |
| `{{people}}` | 聯絡人的人數（`custom_data.party_size`）或「-」若未填寫 |

> **注意：** 若聯絡人資料中沒有結構化的日期/時間欄位（例如只有備註「7pm 2 Jun」），AI 會嘗試從通話記錄中自動提取日期和時間。

---

**查看 WhatsApp 確認狀態：**

在外撥通話報告頁，成功發送 WhatsApp 確認後會顯示「✅ WhatsApp 確認已發送」標籤。

```
[截圖 4.5.3: 通話報告頁，顯示綠色
「✅ WhatsApp 確認已發送」標籤]
```

---

## 5. WhatsApp 建立外撥活動

透過 WhatsApp 可直接與管理機器人對話，快速建立外撥活動，特別適合在外時使用。

### 5.1 開始使用 WhatsApp 機器人

**前置條件：**
- 您的 WhatsApp 號碼已被管理員加入授權名單
- 機器人 WhatsApp 號碼已設定完成

**步驟 4.1.1 — 發送招呼語**

向機器人 WhatsApp 號碼發送「**你好**」。

```
[截圖 4.1.1: WhatsApp 對話截圖，顯示用戶發送「你好」
後，機器人回覆包含三個快速回覆按鈕的歡迎訊息]
```

機器人回覆：
```
👋 你好！請選擇，或直接輸入：

• new餐廳 — 快速建立（可換成其他行業）
• new範本 — 建立新範本

[📞 新活動]  [🔁 重複上次]  [❌ 取消]
```

**按鈕說明：**

| 按鈕 | 說明 |
|------|------|
| **📞 新活動** | 開始逐步建立新活動 |
| **🔁 重複上次** | 複製上次活動配置，直接輸入新聯絡人 |
| **❌ 取消** | 取消目前操作 |

**快捷文字指令：**

| 指令 | 說明 |
|------|------|
| `new餐廳` | 快速建立餐廳範本活動（可替換行業名稱）|
| `new美容院` | 快速建立美容院範本活動 |
| `new保險` | 快速建立保險範本活動 |
| `repeat` | 重複上次活動，輸入新聯絡人 |
| `cancel` | 取消目前操作（隨時可用）|

---

### 5.2 透過 WhatsApp 建立活動

**方法一：快速建立（推薦）**

**步驟 4.2.1 — 輸入快捷指令**

直接輸入 `new餐廳`（或其他行業名稱）：

```
[截圖 4.2.1: 用戶輸入「new餐廳」後，
機器人回覆確認已選擇餐廳範本，並請求輸入聯絡人]
```

機器人回覆：
```
✅ 已選擇「🍽️ 餐廳」

請新增預訂記錄，你可以：
• 傳送聯絡人名單的相片
• 或逐行輸入（每行一位）：
  姓名, 電話, 時間, 日期, 備註
  姓名, 電話（其他欄位可選）

輸入 cancel 可隨時取消。
```

**步驟 4.2.2 — 輸入聯絡人**

**選項 A — 文字輸入：**
```
陳大文,51873117,7pm,30-May,4位
李小明,90218835,6pm,30-May,2位
```

> **提示：** 可使用中文逗號（，）或英文逗號（,）分隔。電話號碼無需輸入 +852，系統會自動加上。

**選項 B — 傳送相片：**
直接傳送聯絡人名單的相片，AI 自動識別並提取資料。

```
[截圖 4.2.2: 用戶上傳聯絡人名單相片後，
機器人顯示「🔍 正在從圖片中提取聯絡人…」
然後顯示提取結果]
```

**步驟 4.2.3 — 確認聯絡人**

機器人顯示提取到的聯絡人列表及確認按鈕：

```
[截圖 4.2.3: 機器人顯示聯絡人確認畫面，
包含編號列表、三個按鈕（✅確認繼續/🚀立即啟動/❌取消）
及自由輸入指令說明]
```

```
✅ 已儲存 2 位聯絡人。

1. ✅ 陳大文 · +85251873117 · 7pm · 30-May · 4位
2. ✅ 李小明 · +85290218835 · 6pm · 30-May · 2位

自由輸入：
• fix N 新數值 — 修改欄位
• del N — 刪除聯絡人
• add 姓名, 電話, 時間, 日期, 備註 — 新增聯絡人

[✅ 確認繼續]  [🚀 立即啟動]  [❌ 取消]
```

**修改聯絡人指令：**

| 指令 | 用法 | 範例 |
|------|------|------|
| `fix N 新數值` | 修改第 N 位的電話號碼 | `fix 1 +85251873117` |
| `del N` | 刪除第 N 位聯絡人 | `del 2` |
| `add 姓名,電話,備註` | 新增一位聯絡人 | `add 王美華,98765432,3位` |

**步驟 4.2.4 — 確認並啟動**

- 點擊「**✅ 確認繼續**」→ 進入排程選擇步驟
- 點擊「**🚀 立即啟動**」→ 立即開始致電（跳過排程步驟）

```
[截圖 4.2.4: 點擊「立即啟動」後，
機器人顯示「🚀 活動已啟動！」訊息及活動連結]
```

成功後機器人回覆：
```
🚀 活動已啟動！2 個通話即時撥出。

查看詳情：
https://business-console.zeabur.app/campaigns/62

輸入 新活動 建立另一個。
```

---

**方法二：逐步建立**

適合需要選擇特定範本（非預設行業）的情況：

**步驟 4.2.5 — 點擊「📞 新活動」按鈕**

```
[截圖 4.2.5: 機器人顯示範本選擇列表（編號1-6），
及底部提示「輸入數字選擇，例如：1」]
```

機器人顯示：
```
🍽️ 選擇活動範本：

1. 🍽️ 餐廳
   您好，我係…嘅Jamie。我哋打嚟確認您…
2. 💇 美容院
   您好，我係…嘅Anna。我哋打嚟確認您…
3. 🛡️ 保險
   ...

輸入數字選擇，例如：1
```

輸入對應數字（例如 `1`）選擇範本，然後按照步驟 4.2.2-4.2.4 繼續操作。

---

**排程致電步驟（如選擇「確認繼續」）：**

```
[截圖 4.2.6: 機器人顯示排程選擇按鈕：
⚡ 立即開始 / 📅 排程]
```

- **⚡ 立即開始** — 確認後立即撥出
- **📅 排程** — 輸入排程時間（格式：`2 YYYY-MM-DD HH:MM`，例如 `2 2026-06-15 09:00`）

---

**重複上次活動（repeat 指令）：**

若需對不同聯絡人重複同樣的活動設定，輸入 `repeat`：

```
[截圖 4.2.7: 輸入 repeat 後，機器人顯示
上次活動名稱及請求輸入新聯絡人的提示]
```

```
🔁 重複活動「晚餐訂座確認」，更換新聯絡人。

語音及腳本與上次相同，請新增聯絡人：
• 傳送聯絡人名單的相片
• 或逐行輸入：姓名, 電話, 時間, 日期, 備註
```

---

### 5.3 透過 WhatsApp 建立範本

建立自訂範本後，可在 WhatsApp 建立活動時直接選用。

**步驟 4.3.1 — 開始建立範本**

輸入 `new範本`：

```
[截圖 4.3.1: 輸入「new範本」後，機器人顯示
第1步語音選擇的列表（含5個語音選項）]
```

機器人回覆：
```
📋 建立新範本 — 第 1 步：選擇語音

1. Jamie (Female Cantonese)
2. Kenji (Male Cantonese)
3. Anna (Female English)
4. Moss
5. Test Voice

輸入數字選擇，例如：1
```

**步驟 4.3.2 — 選擇語音（第 1 步）**

輸入對應數字（例如 `1` 選擇 Jamie）：

```
[截圖 4.3.2: 語音選擇後，機器人確認語音
並顯示第2步語言選擇的按鈕]
```

```
語音：Jamie (Female Cantonese) ✅

第 2 步：選擇語言

[廣東話]  [English]  [Português]
```

**步驟 4.3.3 — 選擇語言（第 2 步）**

點擊語言按鈕（例如「**廣東話**」）：

```
語言：廣東話 ✅

第 3 步：輸入問候語
（AI 接通後第一句說話）

例：你好，我係{{business}}嘅Jamie，
打嚟確認你{{date}}{{time}}嘅訂座。

可用變數：{{name}} {{date}} {{time}} 
{{party_size}} {{business}}

輸入 cancel 可隨時取消。
```

**步驟 4.3.4 — 輸入問候語（第 3 步）**

輸入您的問候語：
```
你好，我係{{business}}嘅Jamie。我哋打嚟確認您{{date}}{{time}}，{{party_size}}位嘅訂座。請問您係咪如期到來？
```

```
[截圖 4.3.4: 輸入問候語後，機器人確認
「問候語已儲存 ✅」並請求輸入範本名稱]
```

**步驟 4.3.5 — 設定範本名稱（第 4 步）**

```
問候語已儲存 ✅

第 4 步：輸入範本名稱
（方便識別，例如：餐廳週末版、美容院VIP）
```

輸入範本名稱（例如：`週末訂座確認`）：

```
[截圖 4.3.5: 範本建立完成訊息，顯示
範本名稱、語音及「輸入 新活動 立即使用」提示]
```

```
✅ 範本已建立！

名稱：週末訂座確認
語音：Jamie

下次建立活動時可選擇此範本。
輸入 新活動 立即使用。
```

範本建立後，透過 WhatsApp 建立活動時，在範本選擇列表中將出現此自訂範本。

---

## 6. 系統管理員（Admin）

管理員帳號可管理平台上所有帳號、查看各帳號的用量報告，並代入任何帳號進行設定。

### 6.1 管理員帳號說明

| 帳號 | 說明 |
|------|------|
| **admin** | 系統管理員，可管理所有帳號，左側導航顯示「Admin」連結 |
| **一般帳號** | 例如 admin123，只能查看自己的資料 |

> **安全提示：** 管理員帳號不應用於日常操作，建議只在需要管理帳號時使用。

---

### 6.2 帳號列表

**步驟 6.2.1 — 進入管理員頁面**

以管理員帳號登入後，點擊左側導航「**Admin**」連結，或直接前往 `/admin`。

```
[截圖 6.2.1: 帳號列表頁面，顯示帳號表格，
包含：帳號名稱、設定狀態、熱線數、活動數、
入線通話數、外撥通話數、建立日期及操作按鈕]
```

**設定狀態說明：**

| 狀態 | 說明 |
|------|------|
| ✅ **Ready** | 所有必要憑證均已設定 |
| ⚠️ **Partial** | 部分憑證已設定，部分缺少 |
| ✗ **Not set** | 尚未設定任何憑證（使用系統環境變數）|

> **注意：** 「Not set」不代表系統無法使用，系統會自動使用伺服器環境變數作為備用。

---

### 6.3 新增帳號

**步驟 6.3.1 — 點擊「New Account」**

在帳號列表頁右上角點擊「**New Account**」按鈕。

```
[截圖 6.3.1: 新增帳號表單，顯示三個欄位：
用戶名、顯示名稱（選填）、密碼]
```

**步驟 6.3.2 — 填寫帳號資料**

| 欄位 | 說明 | 範例 |
|------|------|------|
| **Username** | 帳號登入名稱（不可重複）| restaurant_hk |
| **Display Name** | 顯示名稱（選填）| 香港海景餐廳 |
| **Password** | 登入密碼 | （安全密碼）|

**步驟 6.3.3 — 點擊「Create」**

帳號建立後，將出現在列表中，設定狀態為「Not set」，表示尚未設定憑證。

---

### 6.4 帳號設定（Setup）

每個帳號可獨立設定 Twilio、Gemini 等憑證，以及商戶名稱、WhatsApp 確認等選項。

**步驟 6.4.1 — 進入帳號詳情**

在帳號列表點擊任一帳號的「**View**」按鈕。

**步驟 6.4.2 — Setup 標籤頁**

```
[截圖 6.4.1: 帳號詳情 Setup 標籤頁，顯示
Credentials 和 Settings 兩個卡片]
```

**Credentials（憑證）區塊：**

每個欄位右側會顯示狀態圖示：
- ✅ 綠色 = 已設定
- ⚠️ 橙色 = 未設定（使用伺服器預設值）

| 欄位 | 說明 |
|------|------|
| **Twilio Account SID** | Twilio 帳號 ID（ACxx...）|
| **Twilio Auth Token** | Twilio 驗證 Token |
| **Twilio Phone Number** | 發出外撥電話的號碼（含國碼，如 +85226715377）|
| **Twilio WhatsApp Number** | WhatsApp 發送號碼（含 whatsapp: 前綴或純號碼）|
| **Gemini API Key** | Google Gemini AI 金鑰 |
| **Gemini Model** | Gemini 模型版本（預設 gemini-2.0-flash）|
| **Voice Webhook URL** | voice-claw-webhook 服務網址 |
| **Webhook Base URL** | business-console 本身的網址 |

填寫後點擊「**Save Credentials**」。

> **提示：** 若留空，系統會使用伺服器的環境變數。建議每個帳號都填寫自己的 Twilio 憑證，以便分開帳單。

**Settings（設定）區塊：**

| 欄位 | 說明 |
|------|------|
| **Business Name** | 商戶名稱，用於 AI 問候語及 WhatsApp 確認訊息 |
| **Default Area Code** | 預設電話區碼（預設 +852）|
| **WA Outbound Confirmation** | 外撥確認訂位後自動發送 WhatsApp（開關）|
| **WA Inbound Confirmation** | 入線確認訂位後自動發送 WhatsApp（開關）|
| **New Password** | 更改帳號密碼（留空則不更改）|

填寫後點擊「**Save Settings**」。

---

### 6.5 帳號總覽（Overview）

點擊帳號詳情的「**Overview**」標籤頁，可查看該帳號的所有熱線及活動。

```
[截圖 6.5.1: Overview 標籤頁，顯示 Hotlines 表格
（名稱、電話、狀態、通話數、知識庫文章數）
及 Campaigns 表格（名稱、狀態、聯絡人數、完成數、接聽數）]
```

**Hotlines 表格欄位：**

| 欄位 | 說明 |
|------|------|
| **Name** | 熱線名稱 |
| **Phone** | Twilio 電話號碼 |
| **Status** | active（啟用中）/ paused（已暫停）|
| **Total Calls** | 該熱線的歷史入線通話總數 |
| **KB Items** | 知識庫文章數量 |
| **Live** | 目前正在進行的通話數 |

**Campaigns 表格欄位：**

| 欄位 | 說明 |
|------|------|
| **Name** | 活動名稱 |
| **Status** | 活動狀態 |
| **Contacts** | 聯絡人總數 |
| **Done** | 已完成致電數 |
| **Answered** | 成功接聽數 |

---

### 6.6 帳號報告（Report）

點擊帳號詳情的「**Report**」標籤頁，查看該帳號在特定時期的通話用量報告。

```
[截圖 6.6.1: Report 標籤頁，顯示時間選擇器（7d/30d/90d）、
四個統計卡片（入線/外撥/訂座確認/WA確認）、
入線及外撥分解進度條、每日趨勢圖及通話歷史表格]
```

**時間範圍選擇：**

點擊「**7d**」、「**30d**」或「**90d**」按鈕切換統計時段。

**統計卡片：**

| 卡片 | 說明 |
|------|------|
| **Inbound Calls** | 入線通話總數 |
| **Outbound Calls** | 外撥通話總數 |
| **Bookings Confirmed** | AI 確認訂位次數 |
| **WA Confirmations** | 成功發送 WhatsApp 確認訊息次數 |

**入線分解（Inbound Breakdown）：**

| 結果 | 說明 |
|------|------|
| Resolved | AI 成功解決問題 |
| Escalated | 客人要求轉接真人 |
| Follow-up | 非辦公時間來電，需跟進 |
| Missed | 未接聽或放棄 |

**外撥分解（Outbound Breakdown）：**

| 結果 | 說明 |
|------|------|
| Answered | 客人接聽 |
| No Answer | 未接聽 |
| Voicemail | 留言信箱 |
| Failed | 致電失敗 |

**通話歷史表格：**

顯示最近 50 筆通話記錄（入線及外撥合併），包含類型、電話、來源活動/熱線、日期、時長、結果及 AI 摘要。

---

### 6.7 代入帳號（Impersonate）

管理員可暫時以某個帳號的身份操作平台，查看及管理該帳號的資料。

**步驟 6.7.1 — 點擊「Impersonate」**

在帳號列表或帳號詳情頁，點擊「**Impersonate**」按鈕。

```
[截圖 6.7.1: 代入後的主畫面，頂部顯示橙色橫幅：
「As: admin123」及「Exit」按鈕]
```

代入後：
- 頁面頂部顯示橙色橫幅「**As: [帳號名稱]**」
- 所有操作（查看活動、通話記錄等）均以該帳號的資料為準
- 左側導航的「Admin」連結仍然可見

**步驟 6.7.2 — 退出代入**

點擊橙色橫幅右側的「**Exit**」按鈕，返回管理員身份。

> **用途：** 代入帳號可用於協助客戶設定、排查問題，或查看特定帳號的通話記錄。

---

## 7. 帳號設定（Settings）

每個帳號可在「**設定**」頁面管理自己的商戶資料及 WhatsApp 確認設定。

**步驟 7.1 — 進入設定頁面**

在左側導航點擊「**設定**」。

```
[截圖 7.1: 設定頁面，顯示憑證設定、商戶設定
及 WhatsApp 確認開關]
```

**可設定項目：**

| 項目 | 說明 |
|------|------|
| **Twilio 憑證** | 帳號的 Twilio SID、Token、電話號碼 |
| **Gemini 設定** | AI 模型及 API 金鑰 |
| **Voice/Webhook URL** | 服務端點網址 |
| **商戶名稱** | 用於 AI 問候語及 WhatsApp 訊息 |
| **預設區碼** | 匯入聯絡人時的電話區碼（預設 +852）|
| **WA 外撥確認** | 啟用/關閉外撥通話的 WhatsApp 訂位確認 |
| **WA 入線確認** | 啟用/關閉入線通話的 WhatsApp 訂位確認 |

> **提示：** 若帳號的憑證欄位留空，系統會使用管理員帳號的設定，再向上使用伺服器的環境變數。建議每個帳號填寫自己的 Twilio 憑證以分開帳單。

---

## 8. 常見問題

### Q1: AI 說的名字不正確怎麼辦？

確保在建立活動時，每位聯絡人的「**客人姓名**」欄位已正確填寫，且腳本中使用了 `{{name}}` 變數。

### Q2: 致電時間和地點為什麼是錯的？

檢查以下設定：
1. 活動的「**預訂日期**」和「**預訂時間**」是否正確
2. 腳本中是否使用了 `{{date}}` 和 `{{time}}` 變數
3. 如需個別聯絡人不同時間，需在預訂記錄的「**預訂時間**」欄位單獨設定

### Q3: 如何暫停正在進行的活動？

進入活動詳情頁，點擊右上角「**暫停**」按鈕。正在進行的通話會完成，但不會開始新的通話。

### Q4: WhatsApp 機器人沒有回應怎麼辦？

確認您的 WhatsApp 號碼已被管理員加入授權名單。您可在 Zeabur 後台的 PostgreSQL → `whatsapp_admins` 表格中查看授權號碼。

### Q5: 知識庫文章如何影響 AI 的回答？

AI 會根據知識庫中的文章回答問題。例如，若知識庫中有正確的營業時間，AI 就能準確告知客人。建議定期更新知識庫以確保資訊準確。

### Q6: 如何讓同一客人的訂座記錄不重複致電？

使用「**重設及重試**」功能前，請先確認哪些客人需要重新致電。您可以先「**複製活動**」，然後只輸入需要重致的聯絡人。

### Q7: 客人通話確認後，為什麼沒有收到 WhatsApp 確認訊息？

請逐一檢查以下設定：

1. **設定頁面**：「外撥通話確認」開關是否已開啟（紫色）
2. **活動範本**：「啟用範本 WhatsApp 確認」是否已開啟
3. **聯絡人電話**：電話號碼必須包含國家代碼（例如 +85251873117）
4. **Twilio 範本**：Content Template 是否已審核通過，SID 是否正確
5. **通話結果**：通話報告的「AI outcome」是否顯示為「booking_confirmed」

> **提示：** 在通話報告頁查看「✅ WhatsApp 確認已發送」標籤，確認訊息是否成功發送。

### Q8: WhatsApp 訊息顯示的日期/時間/人數不正確怎麼辦？

系統優先使用聯絡人資料中的結構化欄位（`date`、`time`、`party_size`）。如果您的聯絡人只有備註（例如「7pm 2 Jun」），系統會嘗試從通話記錄中自動提取。

建議在建立活動時，盡量使用「**預訂日期**」和「**預訂時間**」欄位填寫格式化資料（例如 `2026-06-02` 和 `19:00`），而非在備註中輸入自由文字。

### Q9: 如何新增帳號讓其他商戶使用？

需要管理員帳號（admin）登入後，進入 `/admin` 頁面，點擊「**New Account**」按鈕建立新帳號。每個帳號可獨立設定 Twilio 憑證、商戶名稱等，資料完全隔離。

### Q10: 新帳號的通話憑證如何設定？

新帳號建立後設定狀態顯示「Not set」，系統會使用伺服器的環境變數作為備用，通話功能仍可正常使用。如需帳號使用獨立的 Twilio 帳單，請在管理員後台的帳號詳情 → **Setup** 標籤頁填寫該帳號的 Twilio 憑證。

### Q11: 忘記密碼怎麼辦？

聯絡系統管理員（admin 帳號），在管理員後台進入帳號詳情 → **Setup** 標籤頁 → **Settings** 區塊，在「New Password」欄位輸入新密碼後點擊「**Save Settings**」重設。

### Q12: 知識庫 PDF 上傳失敗怎麼辦？

請確認 PDF 是文字型檔案（非掃描圖片）。如 PDF 是由掃描圖片生成，系統無法提取文字。建議改用「方法 A — 文字貼上」，將 PDF 內容複製後貼入文字框匯入。

---

## 附錄：功能速查表

### 外撥活動常用操作

| 操作 | 路徑 |
|------|------|
| 建立新活動 | 主畫面 → + 新增 |
| 管理範本 | 新增活動頁 → 管理範本 |
| 啟用範本 WhatsApp 確認 | 管理範本 → 編輯範本 → 啟用開關 |
| 暫停活動 | 活動詳情 → 暫停按鈕 |
| 查看通話記錄 | 活動詳情 → 報告標籤 |
| 複製活動 | 活動詳情 → ··· → 複製活動 |
| 匯出結果 | 活動詳情 → ··· → 匯出 CSV |

### 入線熱線常用操作

| 操作 | 路徑 |
|------|------|
| 建立新熱線 | 入線 → + 新增熱線 |
| 管理知識庫 | 熱線詳情 → 知識庫標籤 |
| 查看通話報告 | 熱線詳情 → 報告標籤 |
| 查看對話記錄 | 報告 → 需要跟進或最近通話 → 查看對話記錄 |
| 標記跟進狀態 | 報告 → 需要跟進區域 |
| 暫停/啟用熱線 | 熱線詳情 → 暫停/啟用按鈕 |

### WhatsApp 確認常用操作

| 操作 | 路徑 |
|------|------|
| 開啟帳號外撥確認 | 設定 → 外撥通話確認 → 開啟 |
| 開啟帳號入線確認 | 設定 → 入線通話確認 → 開啟 |
| 設定商戶名稱 | 設定 → 商戶名稱 |
| 啟用範本確認 | 管理範本 → 啟用範本 WhatsApp 確認 |
| 查看確認狀態 | 通話報告 → ✅ WhatsApp 確認已發送 標籤 |

### 管理員常用操作

| 操作 | 路徑 |
|------|------|
| 查看所有帳號 | Admin → 帳號列表 |
| 新增帳號 | Admin → New Account |
| 設定帳號憑證 | Admin → View（帳號）→ Setup 標籤 |
| 查看帳號通話報告 | Admin → View（帳號）→ Report 標籤 |
| 代入帳號操作 | Admin → Impersonate（帳號）|
| 退出代入 | 頂部橙色橫幅 → Exit |
| 刪除帳號 | Admin → 刪除圖示（非管理員帳號）|

### WhatsApp 機器人指令速查

| 指令 | 功能 |
|------|------|
| `你好` | 顯示主選單及快速按鈕 |
| `新活動` | 逐步建立新活動 |
| `new餐廳` | 快速建立餐廳活動 |
| `new美容院` | 快速建立美容院活動 |
| `repeat` | 重複上次活動 |
| `new範本` | 建立新範本（4步驟）|
| `cancel` | 取消目前操作 |

---

*文件版本：2026年6月（多帳號版）| 如有任何問題，請聯絡系統管理員*
